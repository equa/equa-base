[project]
name = "equa-base-tools"
version = "0.1.0"
description = "SBOM and tooling scripts for equa-base"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
